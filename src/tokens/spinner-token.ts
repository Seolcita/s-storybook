export type SpinnerSizeProperties = {
  height: string;
  width: string;
};

// Spinner sizes
const spinnerXS: SpinnerSizeProperties = {
  height: '2rem',
  width: '2rem',
};

const spinnerS: SpinnerSizeProperties = {
  height: '2.5rem',
  width: '2.5rem',
};

const spinnerM: SpinnerSizeProperties = {
  height: '3.5rem',
  width: '3.5rem',
};

const spinnerL: SpinnerSizeProperties = {
  height: '4rem',
  width: '4rem',
};

const spinnerXL: SpinnerSizeProperties = {
  height: '4.5rem',
  width: '4.5rem',
};

export default { spinnerXS, spinnerS, spinnerM, spinnerL, spinnerXL };
